
[open]

@ SSD Win 10 + Hibernace. + SATA Kabel + Default notes.

- Export dup. Ao Diam. 
- Store RAW value, display rounded value.
- Re-count round flaw.
- Auto calc.
- Bolt text in result.
- Print superscript.
- Excel broken instance Win10.

[1.9]

General:

-@email round + int(zbytek)
-Default notes.

GUI:

-Ao.PG: [100/80] + px, zbytel int().
-AVA cm2 /AVAi cm2/m2.

Tisk:

- User/footer date HMD only.
- "Sticky" footer:

......................................
 Datum: $date             $MEDICUS_USER 

Dekurz:

-Excel logging crash(1061 no object). "cannot open Excel .log"
- Header (Bold/10):

Echokardiografie(TTE) $date:'

-----------------------------
data

- Zaver = Header(bold) + zaver + footer:

Zaver: ............................................
...................................................
Dne: $date                      MUDr. $MEDUCUS_USER <- or map()

-Result font 9 size.
-Remove footer separator.

[1.8]

General:

- Decimal digits round update(calc only).
- Max/mean calc() fix.
- LAVi-1D,  DDZins calc fix.
- E/Em formula. 
- FAS% = EDA / 100 * ESA formula.
- RWT, LWd index(/BSA), LVs index(/BSA) formula.
- BSA two decimal digit round.
- JSON archiving.

GUI:

- Data input size +2px.
- S-RV & AVAi step down "hole".
- Shorter GUI.
- ESA/EDA units.
- LVD index -> LVd index typo.
- Ao VTI, LVOT-VTI units.
- Catch decimal comma separator. 
- DDZinsp/DZexp -> DDZ insp / DDZ expt typo.
- MVAi-DHT units.

Print:

- Preserve linebreak.
- Detect page overflow.
- Group grey line separator.
- Lower / bold note font.
- Date to user info box.

Dekurz:

- Cleanup on re-run.
- Join label/value/unit to single column.
- Lower data row, higher note row.
- Bold note.
- Group label font 9 size.

[1.7]

Initial version.
