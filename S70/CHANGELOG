
[open]

@ SSD Win 10 + Hibernace. + SATA Kabel + Default notes. + RAID1 vs Storage Spaces.

- Export dup. Ao Diam. 
- Store RAW value, display rounded value.
- Re-count round flaw.
- Auto calc.
- Bolt text in result.
- Print superscript.
- Excel broken instance Win10.

GUI:

-Aorta: Asc-Ao(MM) + Asc-Ao(2D)?
-Aoch: SV/SVi 2x, AR-PHT + AR-SLOPE?

General:

-Static constants.

5 to 4 pseudo code:

$line_offset = 0
$hole = False

for $group
  for $array_index
    if value:
      write_line()
      hole=True
    else:
      line_offset+=1
    endif
    if $line_offset = 4:
      if $hole:
        $row+=1
        $hole=False
      endif
      $line_offset=0
    endif  
  next
next 

[1.9]

General:

-Calc() decimal round loop.
-Dekurz crash fix.
-Default notes.

GUI:

-UID slash.
-Expand input box for "ao.PG".
-AVA cm2 /AVAi cm2/m2 units fix.

Tisk:

-UID slash.
-User,footer date H.M.D only.
-"Sticky" footer.

Dekurz:

-Dekurz header.
-Result header, footer.
-Result font 9 size.
-Remove footer separator.

[1.8]

General:

- Decimal digits round update(calc only).
- Max/mean calc() fix.
- LAVi-1D,  DDZins calc fix.
- E/Em formula. 
- FAS% = EDA / 100 * ESA formula.
- RWT, LWd index(/BSA), LVs index(/BSA) formula.
- BSA two decimal digit round.
- JSON archiving.

GUI:

- Data input size +2px.
- S-RV & AVAi step down "hole".
- Shorter GUI.
- ESA/EDA units.
- LVD index -> LVd index typo.
- Ao VTI, LVOT-VTI units.
- Catch decimal comma separator. 
- DDZinsp/DZexp -> DDZ insp / DDZ expt typo.
- MVAi-DHT units.

Print:

- Preserve linebreak.
- Detect page overflow.
- Group grey line separator.
- Lower / bold note font.
- Date to user info box.

Dekurz:

- Cleanup on re-run.
- Join label/value/unit to single column.
- Lower data row, higher note row.
- Bold note.
- Group label font 9 size.

[1.7]

Initial version.
