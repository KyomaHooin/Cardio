
[TODO]

BIOS: A04 Inspiron 3847

https://dl.dell.com/FOLDER05644058M/1/Inspiron_3647_3847_A11.exe

WD Blue SSD 1TB, WDS100T2B0A

http://wddashboarddownloads.wdc.com/wdDashboard/WesternDigitalSSDDashboardSetup.exe

- Export dup. Ao Diam, SV Vmax.
- 4 x 4 GUI
- "Ao @CRLF LVOT" group name
- Dynamic control/calculation.
- Long Excel clip GUIEdit() do not paste bug.
- Asc-Ao MM/2D decurz/print 2D priority.
- Dynamic E + A = E/A calculation.

[2.2]

Global

-Update default < 50 result.
-Update "other" default note.
-FAC% calculation fix.
-Check history age by days. Default to 30 days.
-Do not convert PV Vmax units.
-LAV-2D, LAVi-2D calculation update.
-Asc-Ao index calculation.
-Anulus-AP/P.cip ratio calc.
-Loop over order struct.
-History message update.

GUI

-Storno button & exit logic.
-S-RV -> Sm-RV typo.
-New input P Area-A4C(PS),T Anulus(TCH), Anulus(AO),Asc-Ao index(AO),Area-A4C(LS), Area-A2C(LS), Anulus-AP(MCH), Anulus-IC(MCH),
 Predni cip(MCH), Anulus-AP/P.cip(MCH).
-Removed line from dekurz.

Dekurz

- Asc-Ao MM/2D decurz/print 2D priority.

Tisk

- Asc-Ao MM/2D decurz/print 2D priority.

[2.1]

Global

-Tune parsing.
-Czech logging, verbosity, minor fixes.
-Note typo.
-Detect null height/weight.
-SV/SVi OR fix.

Dekurz

-Delay, error text.

GUI

-Error label.
-SV/SVi width.
-GuiCtrlCreateInput() -> GuiCrtlCreateEdit().

[2.0]

General:

-Single value in "double value" fix.
-TV max/meanPG -> TV max/meanPG.
-AVA, SV, RAVi, E/A Ratio, FAC%, MVAi-PHT calc() fix.
-Update default note/result.
-User map update.
-VTI LVOT/Ao, MR ERO two decimal round.

GUI:

-Round trailing zero(not history) fix.
-LA-Plax => LA-plax, RVplax => RV-plax typo.
-"Delete note", "Delete result" button.

Print:

-Remove "osetrujici lekar".
-QR code/logo update.
-Header update.

Dekurz:

-Init default update.
-Footer empty line.
-Result bold.
-Header font 11.

[1.9]

General:

-Fix double value detection.
-Fifty().
-QR, logo costantiate.
-Asc-Ao(MM),Asc-Ao(2D), AR-PHT, AR-SLOPE.
-RWT round.
-Calc() decimal round loop.
-Dekurz crash fix.
-Default note.
-Default result.

GUI:

-UID slash.
-Expand input box for "ao.PG".
-AVA cm2 /AVAi cm2/m2 units fix.

Print:

-5 to 4 mapping.
-UID slash.
-User, footer date H.M.D only.
-"Sticky" footer.

Dekurz:

-5 to 4 mapping.
-Dekurz header.
-Result header, footer.
-Result font 9 size.
-Remove footer separator.

[1.8]

General:

- Decimal digits round update(calc only).
- Max/mean calc() fix.
- LAVi-1D,  DDZins calc fix.
- E/Em formula. 
- FAS% = EDA / 100 * ESA formula.
- RWT, LWd index(/BSA), LVs index(/BSA) formula.
- BSA two decimal digit round.
- JSON archiving.

GUI:

- Data input size +2px.
- S-RV & AVAi step down "hole".
- Shorter GUI.
- ESA/EDA units.
- LVD index -> LVd index typo.
- Ao VTI, LVOT-VTI units.
- Catch decimal comma separator. 
- DDZinsp/DZexp -> DDZ insp / DDZ expt typo.
- MVAi-DHT units.

Print:

- Preserve linebreak.
- Detect page overflow.
- Group grey line separator.
- Lower / bold note font.
- Date to user info box.

Dekurz:

- Cleanup on re-run.
- Join label/value/unit to single column.
- Lower data row, higher note row.
- Bold note.
- Group label font 9 size.

[1.7]

Initial version.
